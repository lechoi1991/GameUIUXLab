# 1단계 화면 목록

1. 스팀에서 게임 실행하면 로딩화면에서 로딩기다리고 게임 타이틀로 진입
- 배경화면
- 로딩바
- 로딩진행(글자) %(숫자)

---

2. 게임 타이틀
- 배경화면

1) 방 생성 버튼
- 버튼 클릭하면 방 생성 창 활성화
- 방이름 설정
- 방 비밀번호 설정
- 태그 설정

2) 방 찾기 버튼
- 방 리스트 창 활성화(비번유무, 리스트 번호, 방제목, 방장이름, 방인원, )
- 입장 버튼(방 리스트 중 하나 선택 시 활성화)
- 새로고침 버튼
- 태그 선택
- 방 이름 검색

3) 설정 버튼
- 화면(해상도, 전체/창 모드, FPS)
- 사운드(전체음, 배경음, 시스템음, 플레이어음, 마이크음, 마이크 on/off)
- 조작키 설정
- 변경된 설정 저장 버튼
- 설정 나가기버튼

4) 게임종료 버튼
- 버튼 클릭 시 종료할건지 확인하는 창(종료/취소)

---

3. 게임 로비화면(타이틀에서 방을 생성하거나 찾아서 들어왔을때)

1) 준비완료(방장은 시작)버튼

2) 방 나가기 버튼
- 버튼 클릭 시 방 나갈건지 확인하는 창

3) 맵 설정(방장은 변경 가능하고, 다른 인원은 무슨 맵 선택됐는지 알 수 있음)

4) 게임 모드 설정(방장은 변경 가능하고, 다른 인원은 무슨 모드 선택됐는지 알 수 있음)

5) 팀 별 구분(팀 선택하면 해당 팀으로 플레이어 위치 변경)

6) 방장기능 각 플레이어 강퇴, 팀 이동, 순서변경, 빈자리에 AI봇 생성

---

4. 인게임 화면

1) PlayHud
- 매치 남은 시간(상단 중앙)
- RoleChange 재생성 남은시간(매치 남은시간 밑 조금 작게)
- 각 팀별 플레이 인원(상단 양측, 도둑 체포상태면 플레이인원에서 줄어듬)
- 어느 팀이 도둑이고 어느 팀이 경찰인지(팀별 플레이 인원 옆에 표시)
- 경찰 테이저 조준 시 위치
- 채팅창(위치 이동 가능)
- 미니맵
- 아이템 슬롯 박스(노템전은 박스 안에 X표시)
- 아이템 구매 가능한 상점 창(각 아이템별 기능 및 가격 표시)
- 체포된 도둑 근처에서 E키로 Release나 Rescue 가능 표시
- RoleChange 구조물 근처에서 R키 활성화 가능 표시
- RoleChange 활성화 중 로딩바

2) 정보메뉴(Tab키로 활성화)
- 각 플레이어별 스코어 현황(체포 횟수, 체포된 횟수, Rescue 횟수, RoleChange 활성화 횟수)
- 각 플레이어별 음성 조절버튼
- 각 플레이어별 메시지 음성 차단 버튼
- 플레이어 색상 표시
- 플레이어 체포상태 표시

3) 설정메뉴(Esc키로 활성화)
- 게임으로 돌아가기 버튼
- 화면 설정 버튼(로비설정과 같은화면)
- 음량 설정 버튼(로비설정과 같은화면)
- 키 설정 버튼(로비설정과 같은화면)
- 나가기 버튼

4) ResultScreen
- 승자 팀 정보
- 최종 스코어
- 플레이어 별 스코어


# 2단계 입력 인터페이스

Move : WASD
화면 방향 전환 : 마우스
정보메뉴 : Tab
설정메뉴 : Esc
Cancel : Esc
테이저 발사 : Mouse Left Button 


# 3단계 UI Prefab 후보

- E키, R키 활성화 박스
- RoleChange 활성화 로딩바
- ToastMessage(팀 변경됨)
- 